Install virtualenv ( optional )
virtualenv venv ( if using virtual env )
source venv/bin/activate ( if using virtual env )

pip install pandas
pip install sklearn
pip install matplotlib ( For plotting results )
pip install sqlalchemy

